# Virtual environment
deepseek_env/

# Tokenizer cache
.tiktoken_cache/

# Prepared data (regenerate with `python main.py dataset prepare`)
data/shards/*/
data/raw/*/*
data/processed/*/*
data/instruction/*.jsonl

# Checkpoints (large binaries)
checkpoints/**/*.pt
checkpoints/**/*.pkl
*.bin
checkpoints.zip

# Trained-but-rejected embedding model (kept: the .json evidence of the
# negative result; not kept: the actual weights, since they are not used)

# Local SQLite test/runtime databases (regenerated on demand)
data/financial_data.db
data/vector_store.db
data/auth.db
data/.session_secret
data/approvals.db

# Experiment outputs
eval_*.json

# Python
__pycache__/
*.py[cod]
.env

# Logs
*.log
wandb/
experiments/ai_observability.jsonl
